# use H2 Database
spring.h2.console.enabled=true
#Set logging level for application code to debug
logging.level.nsa.group4.medical=debug
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.datasource.initialization-mode=embedded

# Templates reloading during development
spring.thymeleaf.prefix=file:src/main/resources/templates/
spring.thymeleaf.cache=false

# TODO: Add separate profiles for production and development so that h2 is used during development and Maria/MySQL for production